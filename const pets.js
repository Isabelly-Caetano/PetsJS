const pets = [
    {
        name: 'rex',
        type: 'dog',
        age: 10
    },
    {
        name: 'miau',
        type: 'cat',
        age: 2
    },
    {
        name: 'gulp',
        type: 'fish',
        age: 1
    
    }
]

const eMenorQueCinco = () => {
    return numero < 5
}

const newPets = pets.filter(({age})=> eMenorQueCinco (age) {
    return pets.ago < 5
}) 

console.log(pets)
console.log(newpets)


const pets = [
    {
        name: 'rex',
        type: 'dog',
        age: 10
    },
    {
        name: 'miau',
        type: 'cat',
        age: 2
    },
    {
        name: 'gulp',
        type: 'fish',
        age: 1
    
    }
]

const mapPetNames = pets.map((pet)) = {
    return pets.name
}

console.log (petName)

const forEachpetNames = []  
  forEachpetNames.push(pet.name)
} )

console.log (forEachpetName)


const totalWeitgh = pets.reduce ((total, pet) => {
    if (pet.type ! == 'dog') return total
   console.log('Total : ${Ttotal}')
   console.log('Pet: ${pet.nome}')

   return total + pet.weight
}, { totalAge: 0, totalWeitgh: 0 })

console.log(totalWeitgh)


totalAge: total.totalAge + pet.age,
totalWeitgh: total.totalWeitgh + pet.weight

const totalWeitgh = pets.reduce ((total, pet) => {
    return total = pet + weight
}, 0)

const totalWeitghDogs = dogs.reduce ((total, pet) => {
    return total = pet.weight

    .filter((pet) => {

    })
})

console.log (totalWeitgh)